For working with git bash

![[error while running docker init in bash.png]]
- set Bash alias to avoid manually prepending **winpty** all the time
```bash
echo "alias docker='winpty docker'" >> ~/.bashrc
```

